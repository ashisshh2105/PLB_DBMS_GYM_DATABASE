CREATE DATABASE Gym_Management_System;
USE Gym_Management_System;

# 1 OWNER TABLE( TOP LEVEL)
CREATE TABLE An_Owner (
    owner_id INT PRIMARY KEY AUTO_INCREMENT,
    owner_name VARCHAR(100)
);
DESC AN_OWNER;
SELECT * FROM an_owner;

#2 MANAGER(UNDER OWNER)
CREATE TABLE Manager (
    manager_id INT PRIMARY KEY AUTO_INCREMENT,
    manager_name VARCHAR(100),
    email VARCHAR(100),
    owner_id INT,
    FOREIGN KEY (owner_id) REFERENCES An_Owner(owner_id)
        ON DELETE CASCADE
        ON UPDATE CASCADE
);
DESC MANAGER;

#3 MEMBER( UNDER MANAGER) 
CREATE TABLE Member (
    member_id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(100) NOT NULL,
    phone_number VARCHAR(15),
    specialization VARCHAR(100),
    manager_id INT,
    FOREIGN KEY (manager_id) REFERENCES Manager(manager_id)
        ON DELETE SET NULL
        ON UPDATE CASCADE
);
DESC member;
SELECT * FROM member;

# 4 TRAINER (UNDER MANAGER)
CREATE TABLE Trainer (
    trainer_id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(100),
    specialization VARCHAR(100),
    experience_year INT,
    certification VARCHAR(100),
    join_date DATE,
    manager_id INT,
    FOREIGN KEY (manager_id) REFERENCES Manager(manager_id)
        ON DELETE SET NULL
        ON UPDATE CASCADE
);

-- DERIVED VIEW → Trainer Experience
CREATE VIEW Trainer_Experience AS
SELECT 
    trainer_id,
    name,
    TIMESTAMPDIFF(YEAR, join_date, CURDATE()) AS experience_years
FROM Trainer;

DESC TRAINER;

-- 6 PAYMENT (UNDER MEMBER)  🔥 FIXED ORDER
CREATE TABLE Payment (
    payment_id INT PRIMARY KEY AUTO_INCREMENT,
    payment_date DATE,
    mode VARCHAR(50),
    transaction_status VARCHAR(50),
    member_id INT,
    FOREIGN KEY (member_id) REFERENCES Member(member_id)
        ON DELETE CASCADE
        ON UPDATE CASCADE
);

-- DERIVED VIEW → Total Payments
CREATE VIEW Member_Total_Payments AS
SELECT 
    member_id,
    COUNT(payment_id) AS total_payments
FROM Payment
GROUP BY member_id;

DESC PAYMENT;

-- 5 MEMBERSHIP (UNDER MEMBER)
CREATE TABLE Membership (
    membership_id INT PRIMARY KEY AUTO_INCREMENT,
    start_date DATE,
    status VARCHAR(50),
    member_id INT,
    payment_id INT,
    FOREIGN KEY (member_id) REFERENCES Member(member_id)
        ON DELETE CASCADE
        ON UPDATE CASCADE,
    FOREIGN KEY (payment_id) REFERENCES Payment(payment_id)
        ON DELETE CASCADE
        ON UPDATE CASCADE
);

DESC MEMBERSHIP;

-- 7 WORKOUT (MEMBER + TRAINER)
CREATE TABLE Workout (
    workout_id INT PRIMARY KEY AUTO_INCREMENT,
    difficulty VARCHAR(50),
    status VARCHAR(50),
    exercise_name VARCHAR(100),
    member_id INT,
    trainer_id INT,
    FOREIGN KEY (member_id) REFERENCES Member(member_id)
        ON DELETE CASCADE
        ON UPDATE CASCADE,
    FOREIGN KEY (trainer_id) REFERENCES Trainer(trainer_id)
        ON DELETE SET NULL
        ON UPDATE CASCADE
);

-- DERIVED VIEW → Workout Count
CREATE VIEW Member_Workout_Count AS
SELECT 
    member_id,
    COUNT(workout_id) AS total_workouts
FROM Workout
GROUP BY member_id;

DESC WORKOUT;

-- 8 DIET (MEMBER + TRAINER)
CREATE TABLE Diet (
    diet_id INT PRIMARY KEY AUTO_INCREMENT,
    calories_target INT,
    member_id INT,
    trainer_id INT,
    FOREIGN KEY (member_id) REFERENCES Member(member_id)
        ON DELETE CASCADE
        ON UPDATE CASCADE,
    FOREIGN KEY (trainer_id) REFERENCES Trainer(trainer_id)
        ON DELETE SET NULL
        ON UPDATE CASCADE
);

DESC DIET;

-- 9 EQUIPMENT MAINTENANCE
CREATE TABLE Equipment_Maintenance (
    maintenance_id INT PRIMARY KEY AUTO_INCREMENT,
    equipment_name VARCHAR(100),
    condition_status VARCHAR(100),
    last_service_date DATE,
    incident_date DATE,
    trainer_id INT,
    FOREIGN KEY (trainer_id) REFERENCES Trainer(trainer_id)
        ON DELETE SET NULL
        ON UPDATE CASCADE
);

DESC equipment_maintenance;

-- SHOW ALL TABLES
SHOW TABLES;

SELECT TABLE_NAME, COLUMN_NAME
FROM INFORMATION_SCHEMA.COLUMNS
WHERE TABLE_SCHEMA = 'Gym_Management_System';


TABLES:-

USE Gym_Management_System;

INSERT INTO An_Owner (owner_name) VALUES
('Rohit Singh'),
('Aman Gupta'),
('Suresh Yadav');

INSERT INTO Manager (manager_name, email, owner_id) VALUES
('Rajesh Kumar', 'rajesh@gmail.com', 1),
('Pooja Sharma', 'pooja@gmail.com', 2),
('Vikas Mehta', 'vikas@gmail.com', 3);

INSERT INTO Member (name, phone_number, specialization, manager_id) VALUES
('Amit Verma', '9876543210', 'Weight Loss', 1),
('Neeraj Singh', '9123456780', 'Muscle Gain', 1),
('Riya Sharma', '9988776655', 'Yoga', 2),
('Karan Patel', '9012345678', 'Cardio', 3),
('Sneha Kapoor', '9090909090', 'Strength Training', 2);

INSERT INTO Trainer (name, specialization, experience_year, certification, join_date, manager_id) VALUES
('Arjun Singh', 'Weight Training', 5, 'Certified Trainer', '2020-01-10', 1),
('Meena Joshi', 'Yoga Expert', 3, 'Yoga Certification', '2022-03-15', 2);

INSERT INTO Payment (payment_date, mode, transaction_status, member_id) VALUES
('2026-03-01', 'UPI', 'Completed', 1),
('2026-03-02', 'Cash', 'Completed', 2),
('2026-03-03', 'Card', 'Pending', 3),
('2026-03-04', 'UPI', 'Completed', 4),
('2026-03-05', 'Cash', 'Completed', 5);

INSERT INTO Membership (start_date, status, member_id, payment_id) VALUES
('2026-03-01', 'Active', 1, 1),
('2026-03-02', 'Active', 2, 2),
('2026-03-03', 'Inactive', 3, 3),
('2026-03-04', 'Active', 4, 4),
('2026-03-05', 'Active', 5, 5);

INSERT INTO Workout (difficulty, status, exercise_name, member_id, trainer_id) VALUES
('Medium', 'Completed', 'Bench Press', 1, 1),
('Hard', 'Completed', 'Deadlift', 2, 1),
('Easy', 'Pending', 'Yoga Stretch', 3, 2),
('Medium', 'Completed', 'Cycling', 4, 1),
('Hard', 'Pending', 'Squats', 5, 1);

INSERT INTO Diet (calories_target, member_id, trainer_id) VALUES
(2200, 1, 1),
(2500, 2, 1),
(1800, 3, 2),
(2000, 4, 1),
(2300, 5, 1);

INSERT INTO Equipment_Maintenance (equipment_name, condition_status, last_service_date, incident_date, trainer_id) VALUES
('Treadmill', 'Good', '2026-03-10', NULL, 1);



